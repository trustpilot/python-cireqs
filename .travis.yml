sudo: required

language: python

python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"

services:
  - docker

install:
  - pip install tox
  - pip install tox-travis

script:
  - tox

branches:
  only: master